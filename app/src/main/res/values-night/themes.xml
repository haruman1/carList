<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CarMobile" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/material_red</item>
        <item name="colorPrimaryVariant">@color/material_red</item>
        <item name="colorOnPrimary">@color/light_dark</item>
        <item name="bg">@color/dark</item>
        <item name="boldText">@color/white_bold_text</item>
        <item name="regularText">@color/black_regular_text</item>
        <item name="typing">@color/white_bold_text</item>
        <item name="appbar">@color/dark</item>
        <item name="card">@color/light_dark</item>
        <item name="field">@color/dark_10p</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/material_red</item>
        <item name="colorSecondaryVariant">@color/material_red</item>
        <item name="colorOnSecondary">@color/material_red</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/dark</item>
        <item name="android:windowLightStatusBar">false</item>
        <!-- Customize your theme here. -->
    </style>
</resources>